# Repository Context

## `install.sh`
Interactive and non-interactive dotfiles installer. It bootstraps tool installs, symlinks shared configs and skills into user directories, and now selects the QMD module by default in interactive flows so the knowledge-base search and ingest skills have their required CLI available.
